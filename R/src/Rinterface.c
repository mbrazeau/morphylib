

char(*) matrix
int ntaxa
int nchar
int(*) list_anc
int(*) list_child
int(*) weight


SEXP morphy_handler(SEXP matrix, SEXP ntaxa, SEXP nchar, SEXP list_anc, SEXP list_child, SEXP weight) {

    int nedges
    int nnodes
    char(*) symbols
    

}